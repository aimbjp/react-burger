import React from 'react';import { Logo, ProfileIcon, BurgerIcon, ListIcon } from '@ya.praktikum/react-developer-burger-ui-components';import styles from './app-header.module.css';import { Link, NavLink } from "react-router-dom";import getClasses from "../../utils/functions/getClassesColor";export default function AppHeader() {    return (        <header className={`pb-4 pt-4 text ${styles.header}`}>            <nav className={`pl-5 pr-5 ${styles.nav}`}>                <NavLink                    to='/'                    className={({ isActive }) => getClasses(`pr-2 ${styles.a} text_type_main-default`, isActive)}>                    {({ isActive }) => (                        <>                            <BurgerIcon type={isActive ? "primary" : "secondary"} />                            <span className="pl-2">Конструктор</span>                        </>                    )}                </NavLink>                <NavLink                    to='/list-orders'                    className={({ isActive }) => getClasses(`${styles.a} text_type_main-default`, isActive)}>                    {({ isActive }) => (                        <>                            <ListIcon type={isActive ? "primary" : "secondary"} />                            <span className="pl-2">Лента заказов</span>                        </>                    )}                </NavLink>            </nav>            <Link to='/' children={<Logo />} />            <NavLink                to='/profile'                className={({ isActive }) => getClasses(`pl-5 pr-5 ${styles.a} text_type_main-default`, isActive)}>                {({ isActive }) => (                    <>                        <ProfileIcon type={isActive ? "primary" : "secondary"} />                        <span className={`pl-2`} >Личный кабинет</span>                    </>                )}            </NavLink>        </header>    );}